// TODO: Expand this. Make it cover most of the DerpScript features.

// Stupid test function example.
var testFunc := function(a, b) {
    out("Param 1: " + string(a) + "\n");
    out("Param 2: " + string(b) + "\n");
};

// Function call example.
testFunc("hurf", "blurf");

// var someString = in();
// out("Blagh: " + someString);

// // Intentionally using NON-REFERENCE copy here! (Don't do this, use :=
// // instead!)
// var a = function() {
//     var b = function() {
//         a();
//     };
//     b();
// };
// a();

for(var i = 0; i < 1000000; i++) {
    // out(string(i) + "\n");
}


