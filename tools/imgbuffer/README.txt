Usage: $0 <image.tga> <bufferPrefix>

ExpiredPopsicle's Static Image Buffer Generator 1.1

Generates static bitmap buffers for C/C++ from image data. Very
specifically for small embedded images. Takes TGA file inputs only,
and converts all images to 1-bit black and white.

Output goes to standard output. Redirect to a file as needed.
